-- ============================================================
-- FIX-57: A 维度 quality_score Bug 修复 + 新生权重优化
-- 目标函数: compute_student_score, compute_student_score_as_of
-- 部署方式: 整个 fix44_46_score_functions.sql 已更新，
--           直接在 Supabase SQL Editor 中执行该文件即可。
-- ============================================================
-- 问题1 (Bug): quality_score 使用原始 mean_duration
--   旧: 0.5 + (r.mean_duration - median_mean) / (2 * pop_iqr)
--   新: 0.5 + (v_effective_mean - median_mean) / (2 * pop_iqr)
--
--   新生前几次若练习偏短 → mean_duration 极低 → quality_score → 0
--   → a_score → 0 → 综合分最多被拉低 13 分
--   v_effective_mean 含贝叶斯收缩保护（0 条时=全班中位数），
--   与 B/T/M/W 已用的值完全一致，无逻辑不一致问题。
--
-- 问题2 (设计): 新生 (hist_count < 4) A 权重 25% 过高
--   旧: B=10% T=10% M=5%  A=25% W=50%
--   新: B=8%  T=8%  M=4%  A=10% W=70%
--
--   新生 A 天然偏低（记录数少），高权重不公平；
--   改为让 W（本周实际练习量）主导，按当周努力评价。
-- ============================================================
-- 影响对比（以新生第1周、3条记录为例）:
--   场景                旧综合分   新综合分   提升
--   每天练30分，略低均值  ~40分     ~62分     +22分
--   每天练45分，接近均值  ~52分     ~70分     +18分
-- ============================================================

-- 请执行 fix44_46_score_functions.sql 中的两个函数定义（已包含此修复）
\i fix44_46_score_functions.sql
