[functions.batch-ai-analysis]
verify_jwt = false         # pg_cron 调用时无法携带 JWT，改由 x-batch-secret 做鉴权
# 注意：config.toml 无法直接设置超时，需在 Supabase Dashboard 设置：
# Dashboard → Edge Functions → batch-ai-analysis → Settings → Max Duration → 400s
